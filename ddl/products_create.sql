-- The SQL script is generated for products.csv by ddl_scripts_generator.py
CREATE TABLE IF NOT EXISTS products (
    product_id INTEGER,
     product_code INTEGER,
     product_description TEXT,
    PRIMARY KEY (product_id));
